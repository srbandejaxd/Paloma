{
  "name": "woodpecker",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
