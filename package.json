{
  "name": "woodpecker",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/src/index.js"
  },
  "engines": {
    "node": ">=20.0.0 <22.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
